spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:experimentalassistant_test;MODE=MySQL;NON_KEYWORDS=VALUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: always
      schema-locations: classpath:sql/schema-h2.sql
